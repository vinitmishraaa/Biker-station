# Biker Station — Chrome/Vercel Video Fix

The background is now a browser-compatible H.264 MP4 (`avc1`), muted, 1280x720, 24fps.

The HTML references:
`biker-background-h264.mp4`

Upload the new `index.html` and `biker-background-h264.mp4` to the GitHub repository root and commit them. Vercel will redeploy automatically.

Do not open index.html directly; use the deployed Vercel URL.
